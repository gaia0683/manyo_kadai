<h1>タスク一覧</h1>

<p><%= notice %></p>

<table>
  <tr>
    <th>タスクの名前</th>
    <th>タスクの内容</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
    <td><%= link_to 'タスクの詳細を確認する', task_path(task.id) %></td>
    <td><%= link_to 'タスクを編集する', edit_task_path(task.id) %></td>
  <td><%= link_to 'タスクを削除する', task_path(task.id), method: :delete %></td>
  </tr>
<% end %>
</table>

<%= link_to 'タスクの登録', new_task_path %>

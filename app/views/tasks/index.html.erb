<h1>タスク一覧</h1>

<table>
  <tr>
    <th>タスクの名前</th>
    <th>タスクの内容</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.name %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>

<%= link_to 'タスクの登録', new_task_path %>
